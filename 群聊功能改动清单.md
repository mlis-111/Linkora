# 群聊功能 · 改动清单

> 默认公共群聊 ID：**`group_public`**

---

## 🗄️ 数据库层

**文件：** [init.sql](init.sql)

新增两张表 + 插入默认公共群聊：

```sql
-- 群聊表
CREATE TABLE chat_group (
    group_id VARCHAR(32) PRIMARY KEY,  -- 群聊ID，如 'group_public'
    group_name VARCHAR(100) NOT NULL,
    owner_id INT,                      -- 创建者，公共群为 NULL
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 群成员表
CREATE TABLE group_member (
    group_id VARCHAR(32) NOT NULL,
    user_id INT NOT NULL,
    joined_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (group_id, user_id)
);

-- 插入默认公共群聊
INSERT INTO chat_group (group_id, group_name) VALUES ('group_public', '公共聊天室');
```

---

## 📦 消息协议层

**文件：** [common/messages.py](common/messages.py)

新增消息类型常量：

```python
# 群聊管理
GROUP_CREATE = "group_create"       # 创建群聊
GROUP_CREATE_RESP = "group_create_resp"
GROUP_JOIN = "group_join"           # 加入群聊
GROUP_JOIN_RESP = "group_join_resp"
GROUP_LIST = "group_list"           # 获取群聊列表
GROUP_LIST_RESP = "group_list_resp"
GROUP_MEMBERS = "group_members"     # 获取群成员
GROUP_MEMBERS_RESP = "group_members_resp"
GROUP_INVITE = "group_invite"       # 邀请好友
```

---

## 🖥️ 服务器端

### 1. [server/db/daos.py](server/db/daos.py)

新增 `GroupDAO` 类，实现以下方法：

| 方法 | 说明 |
|------|------|
| `create(group_id, group_name, owner_id)` | 创建群聊 |
| `get_by_id(group_id)` | 查询群聊信息 |
| `add_member(group_id, user_id)` | 添加成员 |
| `remove_member(group_id, user_id)` | 移除成员 |
| `is_member(group_id, user_id)` | 判断是否成员 |
| `list_my_groups(user_id)` | 查询用户加入的所有群聊 |
| `list_members(group_id)` | 查询群成员列表 |
| `list_available(user_id)` | 查询用户未加入的群聊（用于选择加入） |

### 2. [server/core/context.py](server/core/context.py)

在 `_DB` 类中注册新 DAO：

```python
self.groups = GroupDAO(pool)
```

### 3. [server/modules/chat.py](server/modules/chat.py)

修改 `handle_room_chat`：

- 不再硬编码 `PUBLIC_ROOM_ID`
- 根据消息中的 `room_id` 校验发送者是否在该群中
- 只广播给群成员（而非所有在线用户）

### 4. [server/modules/group.py](server/modules/group.py) ✅ 新建

群聊管理模块，注册以下处理器：

| 消息类型 | 处理函数 | 说明 |
|----------|----------|------|
| `GROUP_CREATE` | `handle_create` | 创建群聊，自动将创建者加入 |
| `GROUP_JOIN` | `handle_join` | 选择加入公共群 |
| `GROUP_LIST` | `handle_list` | 返回用户已加入的群列表 + 可加入的群列表 |
| `GROUP_MEMBERS` | `handle_members` | 返回指定群的成员 |
| `GROUP_INVITE` | `handle_invite` | 邀请好友入群 |

### 5. 服务端启动处（router 注册）

增加一行群聊模块注册：

```python
from server.modules import group as group_module
group_module.register(router, ctx)
```

---

## 📱 客户端

### 1. [client/ui/chat_panel.py](client/ui/chat_panel.py)

改动点：

- **对话列表**：显示多个群聊（不再只有一个公共聊天室），群聊和私聊混排
- **切换群聊**：根据 `group_id` 加载对应群聊历史记录
- **发送群消息**：带上 `room_id`（当前群聊的 ID）
- **接收群消息**：`_on_room_chat` 根据 `room_id` 分发到对应对话项
- **默认选中**：不再自动选中公共聊天室，改为显示空态/提示选择

### 2. [client/ui/friend_panel.py](client/ui/friend_panel.py)

改动点：

- 标题栏附近增加 **"＋ 发起群聊"** 按钮
- 点击后弹出创建群聊对话框

### 3. [client/ui/create_group_dialog.py](client/ui/create_group_dialog.py) ✅ 新建

创建群聊对话框：

- 输入群名
- 好友列表（带复选框勾选）
- 确定/取消按钮
- 确定后发送 `GROUP_CREATE` 消息

---

## 🧪 测试

**文件：** [test/unit/djh/test_group.py](test/unit/djh/test_group.py) ✅ 新建

6 种覆盖：

| # | 测试用例 | 说明 |
|---|----------|------|
| 1 | 创建群聊成功 | 创建者自动成为成员 |
| 2 | 加入公共群 | 选择加入 `group_public` |
| 3 | 非成员发群消息被拒 | 校验群成员身份 |
| 4 | 邀请好友入群 | 邀请后好友可查到此群 |
| 5 | 查询我的群列表 | 返回用户加入的所有群 |
| 6 | 重复加入返回错误 | 已在群中再次加入报错 |

---

## 📊 统计

| 类型 | 新增文件 | 修改文件 | 小计 |
|------|---------|---------|------|
| 数据库 | 0 | 1 | 1 |
| 协议层 | 0 | 1 | 1 |
| 服务器 | 1 | 3 | 4 |
| 客户端 | 1 | 2 | 3 |
| 测试 | 1 | 0 | 1 |
| **总计** | **3** | **7** | **10** |
