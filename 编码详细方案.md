# 校园即时通信与文件传输系统 · 编码详细方案

---

## 0. 总体策略（务必先读）

并行开发能不能不互相卡住，关键在三件事：

1. **契约先行、一次冻结**：先把"消息协议 + 消息类型字段 + 加密接口 + DAO 接口 + 上下文 ctx + 面板基类"这套**接口**定下来并冻结。之后所有人只针对接口写实现，不依赖别人的实现细节。
2. **注册式 + 依赖注入，谁也不 import 谁**：服务器端用 `MessageRouter.register(类型, 处理函数)`，每个业务模块**自注册**自己的消息类型；处理函数通过 `session` / `ctx` 拿到一切（在线表、DAO、加密、发送/广播），**业务模块之间零 import**。客户端同理用 `net.on(类型, 回调)` 订阅 + 面板基类插件化。
3. **桩（stub）先行，系统随时能跑**：加密、各业务模块、各面板都先给"空实现/直通桩"，保证**框架一搭好整套程序就能启动联调**，谁的功能没写完也不影响别人跑。

**唯一的偏序**：阶段 0（框架）必须先于阶段 1（功能）。阶段 1 内部，五个人之间**没有先后依赖**。

```
阶段0  朱俊基：冻结 common/* + 服务器框架 + 四个 DAO
       郭玄同：客户端框架（network / base_panel / main_window / app）
        └─ 交付后，整套程序用"桩"即可启动
阶段1  （并行，无先后）
       武家辉：user 模块 + 登录注册面板 + 断开清理
       董钧豪：chat/friend 模块 + 聊天/好友面板 + CryptoUtil 实现
       郭玄同：file 模块 + 文件面板（在线列表本就属于框架）
       董浩楠：ai 模块 + AI 面板
```

---

## 1. 技术栈与依赖

`requirements.txt`：

```
pymysql
DBUtils
customtkinter
pycryptodome
requests
```

数据库建表沿用《系统设计文档》1.6 节的 DDL（`user / friend / message / file_record` 四张表，utf8mb4，message 表两条索引），由朱俊基提供 `init.sql` 一键初始化，并预置 1 个 `is_ai_bot=1` 的 AI 账号与 3 个测试账号。

---

## 2. 工程目录结构

```
campus_im/
├─ requirements.txt
├─ init.sql                      # 建库建表 + 预置账号（朱俊基）
├─ README.md                     # 运行说明（朱俊基）
│
├─ common/                       # ★公共契约层（朱俊基冻结，全员共用）
│  ├─ __init__.py
│  ├─ protocol.py                # 长度前缀 + JSON 编解码
│  ├─ messages.py                # 消息类型常量 + 字段契约 + 构造helper
│  └─ crypto.py                  # CryptoUtil 接口（董钧豪填实现）
│
├─ server/
│  ├─ config.py                  # 配置（端口/DB/AES密钥/AI接口）
│  ├─ app.py                     # ★装配与启动入口（朱俊基）
│  ├─ core/                      # ★服务器框架（朱俊基）
│  │  ├─ server.py               # ChatServer：accept 循环
│  │  ├─ client_handler.py       # ClientHandler：每连接一线程
│  │  ├─ router.py               # MessageRouter：注册式分发
│  │  ├─ online.py               # OnlineRegistry：在线连接表
│  │  ├─ context.py              # ServerContext / DB / 工作线程池
│  │  └─ session.py              # Session：每连接会话
│  ├─ db/                        # ★数据访问层（朱俊基）
│  │  ├─ pool.py                 # DBPool：连接池
│  │  ├─ base_dao.py             # BaseDAO：query/execute
│  │  └─ daos.py                 # UserDAO/MessageDAO/FriendDAO/FileDAO
│  └─ modules/                   # 业务模块（各成员一人一文件，互不交叉）
│     ├─ user.py                 # 武家辉
│     ├─ chat.py                 # 董钧豪
│     ├─ friend.py               # 董钧豪
│     ├─ file.py                 # 郭玄同
│     └─ ai.py                   # 董浩楠
│
├─ client/
   ├─ config.py                  # 服务器地址/端口/AES密钥
   ├─ app.py                     # ★ChatClient 装配（郭玄同）
   ├─ state.py                   # ClientState：当前用户/在线列表/好友缓存
   ├─ core/                      # ★客户端框架（郭玄同）
   │  ├─ network.py              # NetworkClient：收发线程 + 订阅分发
   │  ├─ base_panel.py           # BasePanel：面板基类
   │  └─ main_window.py          # MainWindow：侧栏 + 面板容器 + 在线列表
   └─ ui/                        # 各功能面板（各成员一人一文件）
      ├─ login_window.py         # 武家辉
      ├─ chat_panel.py           # 董钧豪
      ├─ friend_panel.py         # 董钧豪
      ├─ file_panel.py           # 郭玄同
      └─ ai_panel.py             # 董浩楠
│
└─ test/                         # ★测试代码（根目录）
   ├─ unit-testing/              # 单元测试：各成员完成编码后写入
   ├─ integration-testing/       # 集成测试：联调阶段写入
   └─ system-testing/            # 系统测试：系统测试阶段写入
```

**文件归属即冲突边界**：每个人只改自己名下的文件，`app.py` 里那张"模块注册清单"是稳定的、由朱俊基维护，成员不碰。

---

## 3. 公共契约层 `common/`（最先冻结）

### 3.1 `common/protocol.py` —— 帧与粘包处理

```python
import json
import struct

HEADER = 4  # 4 字节长度前缀（网络字节序无符号整数）

def send_msg(sock, msg: dict):
    data = json.dumps(msg, ensure_ascii=False).encode("utf-8")
    sock.sendall(struct.pack(">I", len(data)) + data)

def recv_msg(sock):
    """读取一条完整消息；连接关闭返回 None。"""
    header = _recv_exactly(sock, HEADER)
    if header is None:
        return None
    (length,) = struct.unpack(">I", header)
    body = _recv_exactly(sock, length)
    if body is None:
        return None
    return json.loads(body.decode("utf-8"))

def _recv_exactly(sock, n):
    buf = bytearray()
    while len(buf) < n:
        chunk = sock.recv(n - len(buf))
        if not chunk:            # 对端关闭
            return None
        buf.extend(chunk)
    return bytes(buf)
```

> 注意：同一 socket 可能被多个线程写（业务回包 + 广播），因此**发送必须加锁**——锁在 `ClientHandler.send` / `NetworkClient.send` 里（见 6、7 节），`send_msg` 本身不负责加锁。

### 3.2 `common/messages.py` —— 消息类型与字段契约

```python
class MT:
    REGISTER        = "register"
    REGISTER_RESP   = "register_resp"
    LOGIN           = "login"
    LOGIN_RESP      = "login_resp"
    LOGOUT          = "logout"
    USER_LIST       = "user_list"
    CHAT            = "chat"
    ROOM_CHAT       = "room_chat"
    HISTORY_REQ     = "history_req"
    HISTORY_RESP    = "history_resp"
    FRIEND_ADD      = "friend_add"
    FRIEND_ADD_RESP = "friend_add_resp"
    FRIEND_REMARK   = "friend_remark"
    FRIEND_LIST     = "friend_list"
    FRIEND_LIST_RESP= "friend_list_resp"
    FILE_REQ        = "file_req"
    FILE_RESP       = "file_resp"
    FILE_DATA       = "file_data"
    FILE_END        = "file_end"
    AI_ASK          = "ai_ask"
    AI_ANSWER       = "ai_answer"
    ERROR           = "error"

PUBLIC_ROOM_ID = 1   # 公共聊天室固定房间号

def error(code, message):
    return {"type": MT.ERROR, "code": code, "message": message}
```

**字段契约（客户端/服务器据此对接，不得各自臆造字段）：**

| 类型 | 方向 | 字段 | 说明 |
|---|---|---|---|
| register / login | C→S | `username`, `password` | 注册/登录凭据 |
| register_resp / login_resp | S→C | `ok`(bool), `reason`, 登录附 `user_id`,`nickname`,`online_users`[] | 结果 |
| user_list | S→C | `online_users`:[{`user_id`,`username`,`nickname`}] | 上下线推送 |
| chat | C→S→C | `from`,`to`,`content`(密文),`ts` | 一对一；content 为 AES 密文 |
| room_chat | C→S→广播 | `from`,`content`(密文),`ts` | 公共聊天室 |
| history_req | C→S | `scope`("p2p"/"room"), `target`(对方user_id), `room_id` | 历史查询 |
| history_resp | S→C | `scope`,`records`:[{`from`,`to`,`content`(密文),`ts`}] | 历史结果 |
| friend_add | C→S | `target_username` | 加好友 |
| friend_add_resp | S→C | `ok`,`reason`,`friend`:{`user_id`,`username`} | 结果 |
| friend_remark | C→S | `friend_id`,`remark` | 设置备注 |
| friend_list / friend_list_resp | C→S / S→C | resp 含 `friends`:[{`user_id`,`username`,`remark`}] | 好友列表 |
| file_req | C→S→C | `from`,`to`,`file_name`,`file_size`,`file_id`(服务器回填) | 传输请求 |
| file_resp | C→S→C | `file_id`,`accept`(bool) | 接受/拒绝 |
| file_data | C→S→C | `file_id`,`seq`,`data`(Base64 块) | 数据块 |
| file_end | C→S→C | `file_id`,`status`(1成功/2失败) | 传输结束 |
| ai_ask | C→S | `question`,`ts` | 向 AI 提问 |
| ai_answer | S→C | `answer`,`ts` | AI 回答 |
| error | S→C | `code`,`message` | 错误 |

> `content` 字段约定：**凡在网络上传输的聊天内容一律是 AES 密文（Base64）**；服务器入库时解密存明文，回历史时再加密发出。客户端永远对 `content` 解密后再显示。

### 3.3 `common/crypto.py` —— 加密接口（董钧豪实现）

```python
class CryptoUtil:
    """文本内容 AES 对称加解密（预共享密钥）。董钧豪填实现。"""
    def __init__(self, key: bytes):
        self._key = key

    def encrypt(self, plaintext: str) -> str:
        # TODO(董钧豪): AES(CBC/EAX) 加密，随机 IV，返回 Base64(IV+密文)
        return plaintext      # 桩：实现前明文直通，保证联调可跑

    def decrypt(self, ciphertext: str) -> str:
        # TODO(董钧豪): 解密
        return ciphertext
```

> 桩让"加密"成为可后置的工作：在董钧豪实现前，聊天以明文跑通；实现后无需改动任何调用方。

---

## 4. 配置 `server/config.py`

```python
import os

class Config:
    HOST = "0.0.0.0"
    PORT = 9000

    DB = dict(host="127.0.0.1", port=3306, user="root",
              password=os.getenv("DB_PASSWORD", ""),
              database="campus_im", charset="utf8mb4")

    AES_KEY = os.getenv("AES_KEY", "campus_im_demo_key_32bytes!!").encode()[:32].ljust(32, b"0")

    AI_API_URL = os.getenv("AI_API_URL", "")     # 你自己的 API
    AI_API_KEY = os.getenv("AI_API_KEY", "")     # 仅服务器持有，绝不下发/入库/提交
    AI_MODEL   = os.getenv("AI_MODEL", "gpt-4o-mini")
    AI_USER_ID = 1                                # 预置 AI 账号的 user_id
```

> API Key、AES Key 一律走环境变量/配置，**不写进代码、不进 Git**（`.gitignore` 排除）。

---

## 5. 数据访问层 `server/db/`（朱俊基，框架的一部分）

### 5.1 `pool.py`

```python
import pymysql
from dbutils.pooled_db import PooledDB

class DBPool:
    def __init__(self, cfg: dict):
        self._pool = PooledDB(
            creator=pymysql, maxconnections=10, blocking=True,
            autocommit=True, cursorclass=pymysql.cursors.DictCursor, **cfg)

    def connection(self):
        return self._pool.connection()   # with pool.connection() as conn: ...
```

### 5.2 `base_dao.py`

```python
class BaseDAO:
    def __init__(self, pool):
        self._pool = pool

    def _query(self, sql, args=()):
        with self._pool.connection() as conn:
            with conn.cursor() as cur:
                cur.execute(sql, args)
                return cur.fetchall()

    def _query_one(self, sql, args=()):
        rows = self._query(sql, args)
        return rows[0] if rows else None

    def _execute(self, sql, args=()):
        with self._pool.connection() as conn:
            with conn.cursor() as cur:
                cur.execute(sql, args)
                return cur.lastrowid
```

### 5.3 `daos.py` —— 四个 DAO（接口即《系统设计》表 2-3）

```python
from server.db.base_dao import BaseDAO

class UserDAO(BaseDAO):
    def get_by_username(self, username):
        return self._query_one("SELECT * FROM user WHERE username=%s", (username,))
    def get_by_id(self, user_id):
        return self._query_one("SELECT * FROM user WHERE user_id=%s", (user_id,))
    def exists(self, username):
        return self.get_by_username(username) is not None
    def insert_user(self, username, password_hash, salt, nickname, is_ai_bot=0):
        return self._execute(
            "INSERT INTO user(username,password_hash,salt,nickname,is_ai_bot)"
            " VALUES(%s,%s,%s,%s,%s)", (username, password_hash, salt, nickname, is_ai_bot))

class MessageDAO(BaseDAO):
    def insert(self, msg_type, sender_id, receiver_id, room_id, content):
        return self._execute(
            "INSERT INTO message(msg_type,sender_id,receiver_id,room_id,content)"
            " VALUES(%s,%s,%s,%s,%s)", (msg_type, sender_id, receiver_id, room_id, content))
    def query_p2p(self, uid_a, uid_b, limit=200):
        return self._query(
            "SELECT * FROM message WHERE msg_type=1 AND "
            "((sender_id=%s AND receiver_id=%s) OR (sender_id=%s AND receiver_id=%s)) "
            "ORDER BY sent_at ASC LIMIT %s", (uid_a, uid_b, uid_b, uid_a, limit))
    def query_room(self, room_id, limit=200):
        return self._query(
            "SELECT * FROM message WHERE msg_type=2 AND room_id=%s "
            "ORDER BY sent_at ASC LIMIT %s", (room_id, limit))

class FriendDAO(BaseDAO):
    def add(self, user_id, friend_id, remark=None):
        return self._execute(
            "INSERT IGNORE INTO friend(user_id,friend_id,remark) VALUES(%s,%s,%s)",
            (user_id, friend_id, remark))
    def set_remark(self, user_id, friend_id, remark):
        self._execute("UPDATE friend SET remark=%s WHERE user_id=%s AND friend_id=%s",
                      (remark, user_id, friend_id))
    def list_by_user(self, user_id):
        return self._query(
            "SELECT f.friend_id AS user_id, u.username, f.remark "
            "FROM friend f JOIN user u ON u.user_id=f.friend_id WHERE f.user_id=%s", (user_id,))

class FileDAO(BaseDAO):
    def insert(self, sender_id, receiver_id, file_name, file_size, status=0):
        return self._execute(
            "INSERT INTO file_record(sender_id,receiver_id,file_name,file_size,status)"
            " VALUES(%s,%s,%s,%s,%s)", (sender_id, receiver_id, file_name, file_size, status))
    def update_status(self, file_id, status, done=False):
        sql = ("UPDATE file_record SET status=%s, transferred_at=NOW() WHERE file_id=%s"
               if done else "UPDATE file_record SET status=%s WHERE file_id=%s")
        self._execute(sql, (status, file_id))
```

> 四张表只有 DAO 一个出口、且**只由朱俊基维护**，业务模块通过 `ctx.db.*` 调用，绝不直接写 SQL —— 避免多人改表/SQL 互相冲突。

---

## 6. 服务器框架 `server/core/`（朱俊基）

### 6.1 `online.py` —— 在线连接表（线程安全）

```python
import threading

class OnlineRegistry:
    def __init__(self):
        self._lock = threading.RLock()
        self._by_uid = {}                 # user_id -> ClientHandler

    def add(self, user_id, handler):
        with self._lock: self._by_uid[user_id] = handler
    def remove(self, user_id):
        with self._lock: self._by_uid.pop(user_id, None)
    def is_online(self, user_id):
        with self._lock: return user_id in self._by_uid
    def online_ids(self):
        with self._lock: return list(self._by_uid.keys())

    def send(self, user_id, msg) -> bool:
        with self._lock: h = self._by_uid.get(user_id)
        if h: h.send(msg); return True
        return False

    def broadcast(self, msg, exclude=None):
        with self._lock: targets = list(self._by_uid.items())
        for uid, h in targets:
            if uid != exclude: h.send(msg)
```

### 6.2 `context.py` —— 全局上下文（依赖注入的唯一入口）

```python
from concurrent.futures import ThreadPoolExecutor
from server.db.daos import UserDAO, MessageDAO, FriendDAO, FileDAO
from server.core.online import OnlineRegistry

class _DB:
    def __init__(self, pool):
        self.users    = UserDAO(pool)
        self.messages = MessageDAO(pool)
        self.friends  = FriendDAO(pool)
        self.files    = FileDAO(pool)

class ServerContext:
    def __init__(self, config, pool, crypto):
        self.config  = config
        self.db      = _DB(pool)
        self.online  = OnlineRegistry()
        self.crypto  = crypto
        self.workers = ThreadPoolExecutor(max_workers=4)   # 外部API等阻塞短任务
        self._disconnect_hooks = []

    def on_disconnect(self, fn):           # 业务模块注册"下线清理"
        self._disconnect_hooks.append(fn)

    def fire_disconnect(self, session):
        for fn in self._disconnect_hooks:
            try: fn(session)
            except Exception: pass
```

`ctx` 把**在线表、四个 DAO、加密、工作线程池**全部集中暴露。业务 handler 只认 `ctx`，从不 import 彼此 —— 这是"零偏序"的核心。

### 6.3 `session.py` —— 每连接会话（传给 handler）

```python
class Session:
    def __init__(self, ctx, handler):
        self.ctx = ctx
        self._handler = handler
        self.user_id = None          # 登录前为 None
        self.username = None
    def send(self, msg):             # 回包给"本连接"
        self._handler.send(msg)
    def bind_user(self, user_id, username):
        self.user_id = user_id; self.username = username
```

### 6.4 `router.py` —— 注册式分发

```python
from common.messages import error

class MessageRouter:
    def __init__(self):
        self._handlers = {}

    def register(self, msg_type, fn):
        if msg_type in self._handlers:               # 防止两人抢同一类型
            raise ValueError(f"消息类型重复注册: {msg_type}")
        self._handlers[msg_type] = fn

    def dispatch(self, session, msg):
        fn = self._handlers.get(msg.get("type"))
        if fn is None:
            session.send(error("UNKNOWN_TYPE", f"未知消息类型: {msg.get('type')}"))
            return
        fn(session, msg)             # 统一签名 handler(session, msg)
```

> `register` 对重复类型直接抛错，**启动时**就能暴露"两个人都注册了同一消息类型"的冲突，而不是运行期才发现。

### 6.5 `client_handler.py` —— 每连接一线程

```python
import socket, threading, logging
from common.protocol import send_msg, recv_msg
from common.messages import error
from server.core.session import Session

class ClientHandler(threading.Thread):
    def __init__(self, conn, addr, ctx, router):
        super().__init__(daemon=True)
        self.conn, self.addr = conn, addr
        self.ctx, self.router = ctx, router
        self.session = Session(ctx, self)
        self._send_lock = threading.Lock()

    def send(self, msg):
        with self._send_lock:               # 串行化对本 socket 的写
            try: send_msg(self.conn, msg)
            except OSError: pass

    def run(self):
        try:
            while True:
                msg = recv_msg(self.conn)
                if msg is None:             # 连接关闭/异常
                    break
                try:
                    self.router.dispatch(self.session, msg)
                except Exception:
                    logging.exception("handler error")
                    self.send(error("SERVER_ERROR", "服务器处理异常"))
        except OSError:
            pass
        finally:
            self._cleanup()

    def _cleanup(self):
        if self.session.user_id is not None:
            self.ctx.online.remove(self.session.user_id)   # 框架兜底：在线表一致
            self.ctx.fire_disconnect(self.session)         # 业务：广播下线（武家辉）
        try: self.conn.close()
        except OSError: pass
```

> 断开处理拆成两半，避免冲突：**在线表移除**由框架兜底保证一致；**对外广播下线**由 user 模块注册的钩子负责（武家辉）。

### 6.6 `server.py` —— accept 循环

```python
import socket
from server.core.client_handler import ClientHandler

class ChatServer:
    def __init__(self, ctx, router):
        self.ctx, self.router = ctx, router

    def serve_forever(self):
        srv = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        srv.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
        srv.bind((self.ctx.config.HOST, self.ctx.config.PORT))
        srv.listen(64)
        print(f"server listening on {self.ctx.config.PORT}")
        while True:
            conn, addr = srv.accept()
            ClientHandler(conn, addr, self.ctx, self.router).start()
```

### 6.7 `app.py` —— 装配与注册（这张清单稳定，成员不改）

```python
from server.config import Config
from common.crypto import CryptoUtil
from server.db.pool import DBPool
from server.core.context import ServerContext
from server.core.router import MessageRouter
from server.core.server import ChatServer
from server.modules import user, chat, friend, file, ai   # 五个业务模块

def build():
    cfg = Config()
    ctx = ServerContext(cfg, DBPool(cfg.DB), CryptoUtil(cfg.AES_KEY))
    router = MessageRouter()
    for module in (user, chat, friend, file, ai):
        module.register(router, ctx)        # 每个模块自注册
    return ChatServer(ctx, router)

if __name__ == "__main__":
    build().serve_forever()
```

### 6.8 业务模块统一接口契约（所有 server/modules/*.py 都遵守）

```python
# 每个模块文件只需暴露一个 register(router, ctx)：
def register(router, ctx):
    router.register(MT.XXX, handle_xxx)         # 注册自己负责的消息类型
    # 需要的话：ctx.on_disconnect(on_disconnect)

# 所有处理函数统一签名：
def handle_xxx(session, msg):
    # session.user_id / session.send(...) / session.ctx.online / session.ctx.db / session.ctx.crypto
    ...
```

> 约束：模块**只能**注册"字段契约表"里属于自己的消息类型；处理函数里只用 `session` 与 `session.ctx`，**不得 import 其它业务模块**。

---

## 7. 客户端框架 `client/core/`（郭玄同）

### 7.1 `state.py` —— 共享状态（各写各的切片）

```python
class ClientState:
    def __init__(self):
        self.user_id = None
        self.username = None
        self.online_users = []     # 由"在线列表"维护（郭玄同）
        self.friends = []          # 由好友面板维护（董钧豪）
```

> 约定：谁的功能就写哪一片状态（在线列表→郭玄同写 `online_users`，好友→董钧豪写 `friends`），其余人只读，避免写冲突。

### 7.2 `network.py` —— 收发线程 + 订阅分发 + GUI 线程编组

```python
import socket, threading
from common.protocol import send_msg, recv_msg

class NetworkClient:
    def __init__(self, host, port, tk_root):
        self._host, self._port = host, port
        self._root = tk_root            # 用 after() 把回调切回 GUI 线程
        self._sock = None
        self._send_lock = threading.Lock()
        self._subs = {}                 # type -> [callback]

    def connect(self):
        self._sock = socket.create_connection((self._host, self._port))
        threading.Thread(target=self._recv_loop, daemon=True).start()

    def on(self, msg_type, callback):   # 各面板订阅自己关心的类型
        self._subs.setdefault(msg_type, []).append(callback)

    def send(self, msg):
        with self._send_lock:
            send_msg(self._sock, msg)

    def _recv_loop(self):
        while True:
            msg = recv_msg(self._sock)
            if msg is None:
                self._root.after(0, self._emit, {"type": "__disconnected__"})
                break
            self._root.after(0, self._emit, msg)   # ★切回 GUI 线程再回调

    def _emit(self, msg):
        for cb in self._subs.get(msg.get("type"), []):
            cb(msg)
```

> CustomTkinter/Tkinter **非线程安全**：接收线程绝不能直接碰控件，统一用 `root.after(0, ...)` 把消息投递回 GUI 线程后再回调。这条铁律写进框架，避免每个面板各自踩坑。

### 7.3 `base_panel.py` —— 面板基类（插件化）

```python
import customtkinter as ctk

class BasePanel(ctk.CTkFrame):
    def __init__(self, master, app):
        super().__init__(master)
        self.app = app                  # 通过 app 拿 net / crypto / state
        self.net = app.net
        self.state = app.state
        self.subscribe()

    def subscribe(self):
        """子类在此 self.net.on(类型, 回调) 订阅自己关心的消息。"""
        pass
```

### 7.4 `main_window.py` —— 侧栏 + 面板容器 + 在线列表（郭玄同）

```python
import customtkinter as ctk

class MainWindow(ctk.CTkFrame):
    def __init__(self, master, app):
        super().__init__(master)
        self.app = app
        # 左侧：在线用户列表 + 好友列表入口；右侧：面板容器(self.content)
        # 在线列表订阅 MT.USER_LIST，更新 app.state.online_users 并刷新（郭玄同）
        ...
    def show(self, panel):
        ...        # 把某个面板切到右侧 content 区域
```

### 7.5 `app.py` —— 客户端装配（郭玄同）

```python
import customtkinter as ctk
from client.config import HOST, PORT, AES_KEY
from common.crypto import CryptoUtil
from client.state import ClientState
from client.core.network import NetworkClient
from client.core.main_window import MainWindow
from client.ui.login_window import LoginWindow
from client.ui.chat_panel import ChatPanel
from client.ui.friend_panel import FriendPanel
from client.ui.file_panel import FilePanel
from client.ui.ai_panel import AIPanel

class ChatClient:
    def __init__(self):
        self.root = ctk.CTk()
        self.crypto = CryptoUtil(AES_KEY)
        self.state = ClientState()
        self.net = NetworkClient(HOST, PORT, self.root)
        self.main = MainWindow(self.root, self)
        self.panels = {                 # 一人一面板，互不交叉
            "login":  LoginWindow(self.root, self),
            "chat":   ChatPanel(self.main, self),
            "friend": FriendPanel(self.main, self),
            "file":   FilePanel(self.main, self),
            "ai":     AIPanel(self.main, self),
        }

    def run(self):
        self.net.connect()
        self.root.mainloop()

if __name__ == "__main__":
    ChatClient().run()
```

### 7.6 面板接口契约（所有 client/ui/*.py 都遵守）

```python
class XxxPanel(BasePanel):
    def subscribe(self):
        self.net.on(MT.XXX_RESP, self._on_xxx)     # 只订阅自己关心的类型
    def _on_xxx(self, msg):                         # 已在 GUI 线程，可直接更新控件
        ...
    # 发送：self.net.send({...})；当前用户：self.state.user_id
```

> 面板之间不互相 import、不互相调用；要联动就通过 `self.state`（读）和订阅消息（被动更新）。

---

## 8. 各成员模块（骨架 + 接口，功能自行扩充）

> 下面给出每人**要实现的处理函数/面板、接口签名、关键步骤**。函数体多为注释级骨架，具体逻辑各自完善。**不要改动 §3–§7 的框架与契约**；要新增消息类型须先在 `common/messages.py` 统一加（找朱俊基协调一次）。

### 8.1 武家辉 —— 用户管理 `server/modules/user.py` + `client/ui/login_window.py`

服务器端：注册、登录、登出、断开下线。

```python
import hashlib, os
from common.messages import MT, error

def register(router, ctx):
    router.register(MT.REGISTER, handle_register)
    router.register(MT.LOGIN,    handle_login)
    router.register(MT.LOGOUT,   handle_logout)
    ctx.on_disconnect(on_disconnect)          # 异常断开时广播下线

def handle_register(session, msg):
    ctx = session.ctx
    # 1) 用户名非空/合法校验；ctx.db.users.exists(username) 判重 → 重复回 register_resp(ok=False)
    # 2) 生成 salt，hash = sha256(salt+password)；ctx.db.users.insert_user(...)
    # 3) session.send(register_resp ok=True)
    ...

def handle_login(session, msg):
    ctx = session.ctx
    # 1) ctx.db.users.get_by_username；不存在→login_resp(ok=False,"用户不存在")
    # 2) 校验 hash；不符→"密码错误"
    # 3) ctx.online.is_online(uid)→已在线→"用户已在线"
    # 4) session.bind_user(uid, username); ctx.online.add(uid, session._handler)
    # 5) 向其他人 ctx.online.broadcast(user_list 或上线通知, exclude=uid)
    # 6) session.send(login_resp ok=True, user_id, nickname, online_users=快照)
    ...

def handle_logout(session, msg):
    # ctx.online.remove(uid)；广播下线；可关闭连接
    ...

def on_disconnect(session):
    # 此时框架已把 uid 移出在线表，这里负责对其他人广播下线（user_list 刷新）
    ...
```

客户端 `login_window.py`：登录/注册界面；`subscribe` 订阅 `LOGIN_RESP`/`REGISTER_RESP`；登录成功后写 `state.user_id/username` 并切到主界面。可扩充：记住用户名、输入校验提示。

### 8.2 董钧豪 —— 聊天+好友+加密

`common/crypto.py`：把 `CryptoUtil.encrypt/decrypt` 用 pycryptodome 的 AES 实现（随机 IV，Base64）。

`server/modules/chat.py`：

```python
from common.messages import MT, PUBLIC_ROOM_ID, error

def register(router, ctx):
    router.register(MT.CHAT,        handle_chat)
    router.register(MT.ROOM_CHAT,   handle_room_chat)
    router.register(MT.HISTORY_REQ, handle_history)

def handle_chat(session, msg):
    ctx = session.ctx
    cipher = msg["content"]                       # 网络上是密文
    plain  = ctx.crypto.decrypt(cipher)           # 入库存明文
    ctx.db.messages.insert(1, session.user_id, msg["to"], None, plain)
    if not ctx.online.send(msg["to"], msg):       # 在线→转发密文原样
        session.send(error("OFFLINE", "对方不在线"))  # 离线→提示（消息已入库）

def handle_room_chat(session, msg):
    ctx = session.ctx
    plain = ctx.crypto.decrypt(msg["content"])
    ctx.db.messages.insert(2, session.user_id, None, PUBLIC_ROOM_ID, plain)
    ctx.online.broadcast(msg)                     # 广播密文

def handle_history(session, msg):
    ctx = session.ctx
    # scope=p2p → query_p2p(user_id, target)；scope=room → query_room(room_id)
    # 把每条 content 用 ctx.crypto.encrypt 加密后放进 history_resp.records
    ...
```

`server/modules/friend.py`：`friend_add`（按用户名查 → ctx.db.friends.add → 回 friend_add_resp）、`friend_remark`、`friend_list`。

客户端：`chat_panel.py`（私聊/公共聊天室收发与展示，发送前 `self.app.crypto.encrypt(content)`，收到后 `decrypt` 再显示；历史记录查询展示）、`friend_panel.py`（好友列表、加好友、设备注，写 `state.friends`）。

### 8.3 郭玄同 —— 文件传输 `server/modules/file.py` + `client/ui/file_panel.py`（在线列表已在框架）

```python
from common.messages import MT, error

def register(router, ctx):
    router.register(MT.FILE_REQ,  handle_file_req)
    router.register(MT.FILE_RESP, handle_relay)    # 应答原样转发
    router.register(MT.FILE_DATA, handle_relay)    # 数据块原样转发
    router.register(MT.FILE_END,  handle_file_end)

def handle_file_req(session, msg):
    ctx = session.ctx
    if not ctx.online.is_online(msg["to"]):
        session.send(error("OFFLINE", "对方不在线")); return
    file_id = ctx.db.files.insert(session.user_id, msg["to"], msg["file_name"], msg["file_size"])
    msg["file_id"] = file_id
    ctx.online.send(msg["to"], msg)                # 转发请求给接收方

def handle_relay(session, msg):
    # file_resp / file_data：根据 to/from 找对端连接，原样中转
    ...

def handle_file_end(session, msg):
    # ctx.db.files.update_status(file_id, status, done=(status==1))；中转 file_end
    ...
```

客户端 `file_panel.py`：选文件→发 file_req；收到 file_resp 决定是否开始；发送方按 64KB 分块、Base64 后逐块发 file_data，更新进度=已发/总大小；接收方落盘并更新进度；完成发 file_end。可扩充：拖拽、保存目录选择。

### 8.4 董浩楠 —— AI 问答 `server/modules/ai.py` + `client/ui/ai_panel.py`

```python
import requests
from common.messages import MT

def register(router, ctx):
    router.register(MT.AI_ASK, handle_ai_ask)

def handle_ai_ask(session, msg):
    session.ctx.workers.submit(_do_ask, session, msg)   # 丢给工作线程，不阻塞连接

def _do_ask(session, msg):
    ctx = session.ctx
    # 1) 入库提问：ctx.db.messages.insert(1, session.user_id, ctx.config.AI_USER_ID, None, question)
    # 2) 调外部 API（OpenAI 兼容）：
    #    requests.post(AI_API_URL, headers={"Authorization": f"Bearer {AI_API_KEY}"},
    #                  json={"model": AI_MODEL, "messages":[{"role":"user","content":question}]})
    # 3) 取 answer；入库回答（sender=AI_USER_ID, receiver=user）
    # 4) session.send({type: ai_answer, answer, ts})
    # 注意异常：超时/失败时 session.send 友好错误，不要让线程崩
    ...
```

客户端 `ai_panel.py`：把 AI 当一个特殊联系人；输入问题→发 ai_ask；订阅 ai_answer 展示。可扩充：加载中状态、多轮上下文。

---

## 9. 防冲突 / 零偏序——关键约定汇总

| 约定 | 作用 |
|---|---|
| `common/*`（协议、消息字段、加密接口）**一次冻结** | 所有人对同一份契约编程，不再相互等待 |
| 服务器**注册式分发** + `register` 重复即报错 | 模块自注册，无中央 switch 可改；抢同名类型启动即暴露 |
| **`ctx` 依赖注入**（在线表/DAO/加密/线程池） | 业务模块之间零 import，彻底解耦 |
| **DAO 单一所有者**（朱俊基），业务走 `ctx.db.*` | 没有多人同时改表/SQL 的冲突 |
| 客户端 **`net.on` 订阅 + BasePanel 插件** | 面板互不 import；联动只经 `state`(读)+消息(被动) |
| **`state` 分片所有权**（谁的功能写哪片） | 共享状态无写冲突 |
| **桩先行**（crypto/模块/面板都能空跑） | 框架一就绪整套程序即可启动，功能可后置 |
| **一人一文件**（modules/*、ui/*） | 文件即冲突边界，Git 几乎不冲突 |
| 新增消息类型须先改 `common/messages.py` | 唯一需要"协调一次"的点，由朱俊基统一加 |

---

## 10. 测试目录与组织

根目录 `test/` 按测试层次分三层，统一用 `pytest` 运行：

- `test/unit-testing/`：**单元测试**。各成员**完成自己模块编码后**写入，针对单个函数/类隔离测试（用桩或假对象代替依赖，可不连数据库）。例如 `protocol` 帧编解码往返、`CryptoUtil` 加解密往返、各 DAO 的增删查、各 handler 用"假 session"调用后的回包。文件名 `test_<模块>.py`。
- `test/integration-testing/`：**集成测试**。**联调阶段**写入，针对"多个模块协作"的链路，需起一个连测试库的服务器、用一两个客户端走完整流程。例如 登录→私聊→入库→历史查询、文件分块收发、AI 提问到回答。
- `test/system-testing/`：**系统测试**。**系统测试阶段**写入，面向《需求分析》验收标准 AC-01~AC-14 的端到端场景：多客户端并发、上下线一致、异常断开服务器不崩、抓包为密文等。

> 单元测试尽量不依赖数据库（用假对象）；集成/系统测试连一个独立测试库 `campus_im_test`，避免污染演示数据。运行示例：`pytest test/unit-testing -v`。

---

## 11. 联调与运行（分阶段 Runbook）

> 总目标：**阶段 0 框架搭好后，用一套"最小冒烟桩"就能跑通最小闭环——连接 → 登录 → 进入主界面 → 看到在线列表。** 之后每个阶段都在"上一阶段可运行"的基础上推进，任何人未完成都不阻塞整体能跑。

### 阶段 0 ｜ 搭框架 + 冒烟跑通（朱俊基 + 郭玄同）

**第 1 步 · 环境准备**

```bash
pip install -r requirements.txt
mysql -u root -p < init.sql            # 建库建表 + 预置 3 个测试账号 + 1 个 AI 账号(user_id=1)
# 设置环境变量（Windows: set / PowerShell: $env:）
export DB_PASSWORD=你的MySQL密码
export AES_KEY=任意32字节字符串
export AI_API_URL=            # 阶段0可留空
export AI_API_KEY=
```

**第 2 步 · 写完框架**：完成 §3–§7 的 `common/*`、`server/db/*`、`server/core/*`、`server/app.py`、`client/core/*`、`client/app.py`、`client/state.py`。

**第 3 步 · 放最小冒烟桩**（保证能登录进主界面，成员阶段 1 直接替换为真实实现）：

- `server/modules/user.py`（登录冒烟桩，不校验密码，DB 查到用户即放行）：

```python
from common.messages import MT

def _snapshot(ctx):
    out = []
    for uid in ctx.online.online_ids():
        u = ctx.db.users.get_by_id(uid)
        if u: out.append({"user_id": uid, "username": u["username"]})
    return out

def register(router, ctx):
    router.register(MT.LOGIN, handle_login)
    router.register(MT.REGISTER, handle_register)
    ctx.on_disconnect(on_disconnect)

def handle_login(session, msg):
    ctx = session.ctx
    row = ctx.db.users.get_by_username(msg.get("username", ""))
    if not row:                                          # 冒烟阶段不校验密码
        session.send({"type": MT.LOGIN_RESP, "ok": False, "reason": "用户不存在"}); return
    uid = row["user_id"]
    session.bind_user(uid, row["username"])
    ctx.online.add(uid, session._handler)
    snap = _snapshot(ctx)
    session.send({"type": MT.LOGIN_RESP, "ok": True, "user_id": uid,
                  "nickname": row.get("nickname") or row["username"], "online_users": snap})
    ctx.online.broadcast({"type": MT.USER_LIST, "online_users": snap})

def handle_register(session, msg):
    session.send({"type": MT.REGISTER_RESP, "ok": True})  # 冒烟桩

def on_disconnect(session):
    session.ctx.online.broadcast({"type": MT.USER_LIST, "online_users": _snapshot(session.ctx)})
```

- 其余 `server/modules/{chat,friend,file,ai}.py` 先放空注册：

```python
def register(router, ctx):
    pass     # 阶段1由各模块负责人补齐
```

- `client/ui/login_window.py`（登录冒烟桩）：

```python
import customtkinter as ctk
from common.messages import MT
from client.core.base_panel import BasePanel

class LoginWindow(BasePanel):
    def subscribe(self):
        self.net.on(MT.LOGIN_RESP, self._on_login)
        self._build()
    def _build(self):
        self.u = ctk.CTkEntry(self, placeholder_text="用户名"); self.u.pack(pady=6)
        self.p = ctk.CTkEntry(self, placeholder_text="密码", show="*"); self.p.pack(pady=6)
        ctk.CTkButton(self, text="登录", command=self._login).pack(pady=6)
    def _login(self):
        self.net.send({"type": MT.LOGIN, "username": self.u.get(), "password": self.p.get()})
    def _on_login(self, msg):
        if msg.get("ok"):
            self.state.user_id = msg["user_id"]; self.state.username = msg.get("nickname")
            self.app.main.show_main()        # 切到主界面
        else:
            print("登录失败:", msg.get("reason"))
```

- 其余 `client/ui/{chat,friend,file,ai}_panel.py` 先放空面板：

```python
from client.core.base_panel import BasePanel
class XxxPanel(BasePanel):
    def subscribe(self): pass
```

- `client/core/main_window.py` 需提供 `show_main()`，并订阅 `MT.USER_LIST` 把 `state.online_users` 渲染到侧栏（这是郭玄同框架本职）。

**第 4 步 · 冒烟跑通**

```bash
python -m server.app        # 终端1：服务器
python -m client.app        # 终端2：客户端 A
python -m client.app        # 终端3：客户端 B
```

A、B 分别用预置测试账号登录。

**✅ 阶段 0 验收（必须达到）**：两个客户端都能登录、进入主界面、在线列表里互相看得到对方。达到即说明**框架可用，正式冻结 `common/*` 与 `core/*`**。

### 阶段 1 ｜ 并行实现功能 + 单元测试（五人并行，互不依赖）

1. 各自把名下 `server/modules/*.py`、`client/ui/*.py` 从桩补成真实功能（武家辉同时把登录桩换成真实加盐哈希校验）。
2. 每完成一块，立刻在 `test/unit-testing/` 写单元测试（DAO、crypto、protocol、各 handler 的假 session 测试）。
3. 自测：起 1 个服务器 + 2 个客户端，验证本人功能。
4. **产出 → `test/unit-testing/`**。因为大家都对着冻结的契约写、且一人一文件，期间随时 `git pull` 都能跑通已完成的部分。

### 阶段 2 ｜ 集成联调 + 集成测试（全员）

1. 合并全部模块，按链路联调：登录→私聊→入库→历史；公共聊天室广播；好友与备注；文件传输与进度；AI 问答。
2. 修跨模块的字段/时序问题（若要加消息类型，先在 `common/messages.py` 统一加，朱俊基统稿）。
3. 在 `test/integration-testing/` 写集成测试（连测试库、起服务器、两客户端走完整链路并断言结果）。
4. **产出 → `test/integration-testing/`**。

### 阶段 3 ｜ 系统测试（全员）

1. 按验收标准 AC-01~AC-14 端到端跑：注册/登录/重复登录、在线列表上下线、私聊/群聊/历史、文件传输与进度、好友与备注、文本加密、AI 问答。
2. 健壮性：直接杀掉一个客户端进程模拟异常断开，确认服务器不崩、其他人在线列表更新；Wireshark 抓包确认 `content` 为密文。
3. 在 `test/system-testing/` 写系统测试场景脚本。
4. **产出 → `test/system-testing/`**，并据此整理《最终项目文档》的系统测试章节。

### 日常运行命令（写进 README）

```bash
pip install -r requirements.txt
mysql -u root -p < init.sql
export DB_PASSWORD=... AES_KEY=... AI_API_URL=... AI_API_KEY=...
python -m server.app          # 启动服务器
python -m client.app          # 启动任意多个客户端
pytest test/unit-testing -v   # 跑测试（integration / system 同理）
```

---

### 给朱俊基（你）的落地顺序建议

1. 先写 `common/protocol.py`、`common/messages.py`（含字段契约表）、`common/crypto.py`（桩）——**当天冻结并通知全员**。
2. 再写 `db/*` + `core/*` + `app.py`，让服务器能用空模块启动。
3. 给每个 `server/modules/*.py`、`client/ui/*.py` 建好**只含 `register`/类骨架的空文件**并提交，确保大家 `git pull` 就有壳子可填。
4. 和郭玄同对一次客户端 `app.py` 装配，确认 `net.on` / `BasePanel` / `state` 三个接口面板侧好用。
5. 之后你专注：服务器核心稳定性、DAO、界面美化、README、统稿。
