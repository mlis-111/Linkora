# 好友申请功能 — 需求说明

## 我要做什么

当前加好友是输入用户名→直接双向添加。改为：
1. 按用户 ID 搜索
2. 加好友需要对方同意

## 需要配合的部分

### 1. 数据库 — 新增 friend_request 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK AUTO_INCREMENT | 主键 |
| from_id | INT NOT NULL | 申请人ID |
| to_id | INT NOT NULL | 接收人ID |
| status | TINYINT DEFAULT 0 | 0=待处理 1=已同意 2=已拒绝 |
| created_at | TIMESTAMP | 创建时间 |

`to_id` 加索引。

### 2. 消息协议 — 新增 6 个常量

```
FRIEND_REQ_NOTIFY   = "friend_req_notify"     # 通知接收方有人申请
FRIEND_AGREE        = "friend_agree"          # 同意申请
FRIEND_AGREE_RESP   = "friend_agree_resp"     # 同意结果
FRIEND_REJECT       = "friend_reject"         # 拒绝申请
FRIEND_REQ_LIST     = "friend_req_list"       # 查询待处理申请
FRIEND_REQ_LIST_RESP = "friend_req_list_resp" # 待处理申请列表
```

### 3. DAO — 新增 FriendRequestDAO

需要提供：创建申请、查是否已申请、查待处理列表（含申请人名称）、更新状态。

### 4. Context — 注册 DAO

把 FriendRequestDAO 挂到 ctx.db 下，参考 ctx.db.friends 的写法。
